#include<iostream>
#include "Header.h"
using namespace std;
int main()
{
	int real, img;
	complex c();
	cout << "Enter the real part then click ENTER then Enter the img part:/t";
	cin >> real >> img;
	c.readcomplex(real,img);
	system("pause");
	return 0;
}
